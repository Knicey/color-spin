#include <iostream> 

int main()
{
  int color;
  int color2;
  int rotate;
  
  color = /camera input/
  color2 = /player input/
  
  if ( color == r ) {
  if ( color2 == y ) {
  rotate = .125
  }
  else if ( color2 == c ) {
      rotate = .25 )
      rotate = -.125
  }	
  }
  else if ( color == y ) {
    if ( color2 == r ) {
      rotate = -.125
    }
    else if ( color2 == c ) {
      rotate = .125
      rotate = .25
    }
    if ( color == c ) {
      if ( color2 == r ) {
        rotate = .25
      }
      else if ( color2 == y ) {
        rotate = -.125
        rotate = .125
      }
    }
    else {
      if ( color2 == r ) {
        rotate = .125
      }
      else if ( color2 == y ) {
        rotate = .25
        rotate = -.125
      }
    }
  }
}
